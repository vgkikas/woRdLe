We wanted to test diff sizes of subsets.

Here are subsets of diff sizes from 50 to 2000, but every next subset uses words already present in the previous one (for example the 100' words subset contains the 50 first words of 50' subset).

All of these subsets are created from "answers.txt".
